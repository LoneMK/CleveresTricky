[package]
name = "daemon"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
libc = "0.2"
neli = "0.6" # For Netlink Process Connector
tokio = { version = "1.0", features = ["full"] }
log = "0.4"
android_logger = "0.13"
